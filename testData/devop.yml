#devPort: 8888
refresh: .1s
commands:
  build:
    match: ".go$"
    command: "go build -o=testcmd ."
    continue: runapp
    wait: true
    stderr: true
    stdout: true
  runapp:
    match: "test.txt$"
    command: "./testcmd"
    kill: true
    onexit: "rm testcmd test.txt test.out.txt"
